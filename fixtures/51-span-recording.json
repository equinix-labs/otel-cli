{
  "description": "otel-cli span sends a span",
  "spans_expected": 1,
  "server_timeout_ms": 1000,
  "config": {
    "env": {
      "OTEL_EXPORTER_OTLP_ENDPOINT": "{{endpoint}}"
    },
    "cli_args": ["span", "--service", "main_test.go", "--name", "test-span-123", "--kind", "server"]
  },
  "expect": {
    "output": "",
    "span_data": {
      "trace_id": "*",
      "span_id": "*",
      "is_sampled": "true"
    }
  }
}
