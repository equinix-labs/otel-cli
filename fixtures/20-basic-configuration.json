{
  "name": "no configuration at all",
  "_comment": "otel-cli should not do anything when it is not explicitly configured",
  "spans_expected": 0,
  "timeout": 0,
  "should_timeout": false,
  "config": {
    "env": {
      "OTEL_EXPORTER_OTLP_ENDPOINT": "localhost:4443"
    },
    "args": []
  },
  "expect": {
    "env": {
      "OTEL_EXPORTER_OTLP_ENDPOINT": "localhost:4443"
   },
    "config": {
      "endpoint": "",
      "insecure": false,
      "service_name": "otel-cli"
    },
    "span_data": {
      "trace_id": "*",
      "span_id": "*",
      "trace_flags": "01",
      "is_sampled": "true"
    }
  }
}
